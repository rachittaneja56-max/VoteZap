# VoteZap frontend — copy to `.env` for local dev

# Base URL of the VoteZap API (no trailing slash). OAuth client IDs and redirect URI
# are loaded from GET /api/auth/public-config using this origin.
VITE_API_URL=http://localhost:5000
